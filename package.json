{
  "name": "Wormenkwekerij-wasse",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "yarn concurrently \"cd vendure && yarn serve\" \"cd storefront && sleep 15s && yarn serve:local\""
  },
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}